/**
 * App shell.
 *
 * Intentionally minimal: the foundational layer ships the data structures,
 * the scientific season matrix, and the persistent global state. Visual
 * composition is layered on top in a later phase.
 */
export default function App() {
  return null;
}
